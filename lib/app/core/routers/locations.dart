class Locations {
  String latLon(String latLon) {
    return '?' + latLon;
  }

  String city(String city) {
    return '?' + city;
  }

  String yourLocation(String yourLocation) {
    return '?' + yourLocation;
  }
}
